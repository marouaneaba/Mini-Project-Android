<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abk.tps">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".InsertPersonne"
            android:label="@string/creer"/>

        <activity android:name=".VisualiserList"
            android:label="@string/visualiser"/>

        <activity android:name=".AfficherContact"
            android:label="@string/AfficherContact"/>
        <activity android:name=".ModifierContact"
            android:label="@string/ModifierContact"/>
        <activity android:name=".RechercherContact"
            android:label="@string/RechercherContact"/>

        <!-- <activity android:name=".InsertPersonne">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> -->
    </application>

</manifest>